#!/bin/sh --

# This is good for viewing, need a way to pull them in once we're in less
lessit() {
	while read -r in; do
		cd "$HOME/local/src/$in"
		st -e git diff HEAD origin/HEAD --color | less -R
	done
}

dmenu -p "Repo to view: " < "$XDG_DATA_HOME"/pulls | lessit
